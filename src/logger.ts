import * as fs from 'fs';

const filePath = 'E:\\Logs\\Output-' + Math.random() * 1000000 + '.json';


export function log(message: string){
    //fs.appendFileSync(filePath, message + '\n');

    //console.log(message);
}